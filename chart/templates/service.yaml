apiVersion: v1
kind: Service
metadata:
  name: {{ include "kenya-forecasts.fullname" . }}
  labels:
    {{- include "kenya-forecasts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kenya-forecasts.selectorLabels" . | nindent 4 }}
